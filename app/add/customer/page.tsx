import { FC } from "react";

import { SaleForm } from "./customerForm";

const page: FC = ({}) => {
  return (
    <div className="space-y-3">
      <SaleForm />
    </div>
  );
};

export default page;
