import { FC } from "react";

import { CustomerForm } from "./customerForm";

const page: FC = ({}) => {
  return (
    <div className="space-y-3">
      <CustomerForm />
    </div>
  );
};

export default page;
