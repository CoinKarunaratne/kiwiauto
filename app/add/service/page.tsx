import { FC } from "react";
import { ServiceForm } from "./serviceForm";

const page: FC = ({}) => {
  return (
    <div className="space-y-3">
      <ServiceForm />
    </div>
  );
};

export default page;
